# tests

Unit tests for Agent Wiki.

The tests cover lifecycle setup, workspace discovery state, onboarding probes, and other deterministic CLI behavior. Run them from the repository root with:

```bash
python3 -m unittest discover -s tests
```
